# frozen_string_literal: true

source "https://rubygems.org"

# Development dependencies
gem "minitest", "~> 5.25"
gem "pry", "~> 0.15.2"

# These are added by the nixx derivation so they're available when bootstrapping
# a new machine. Version numbers match the nixpkgs version.
# See src/base.nix if changing these.
gem "activesupport", "~> 7.2.2"
gem "rake", "~> 13.2.1"
gem "thor", "~> 1.3.2"